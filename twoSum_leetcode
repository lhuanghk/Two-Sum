def twoSum(self, nums, target):
    """
    :type nums: List[int]
    :type target: int
    :rtype: List[int]
    """
    i = 0
    dictNums = {}

    for a in nums:
        b = target - a
        if b in dictNums:
            return [dictNums[b],i]
        dictNums[a] = i
        i = i+1
    return []
